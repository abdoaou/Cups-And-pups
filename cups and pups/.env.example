# --- Cups & Pups static site (copy to `.env`) ---
PORT=3000
ADMIN_PATH=/hidden-admin
API_PROXY_TARGET=https://abnodejsapi-production.up.railway.app

# --- Railway NodeAPI (Supabase) — see ../railway.env.example ---
# The browser never uses DATABASE_URL or JWT_*; only the API on Railway does.
